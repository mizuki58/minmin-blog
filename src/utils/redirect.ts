// import { useNavigate } from "react-router-dom";


// export const redirectTo404 = (dateString: string | undefined): string => {
//     const navigate = useNavigate();
    
    
//     return dateString ? format(parseISO(dateString), "MMMM dd, yyyy") : "";
//   };